# 画像編集 (sdo-edit-pic)

Windows向けの画像編集ソフト。現状はJPG/PNG相互変換機能を提供する。

## 動作環境

- Windows 10 / 11

## 使い方

1. `画像編集.exe` をダブルクリックして起動する。
2. 「画像を選択...」ボタンをクリックし、変換したいPNGまたはJPG画像を選ぶ。
3. PNG画像を選択した場合、JPEG品質スライダーで変換後の品質を調整できる。
4. 「変換して保存」ボタンをクリックする。
5. 変換されたファイルは `画像編集.exe` と同じフォルダに、元のファイルと同じ名前で拡張子だけ変わった状態で保存される。画面には保存先のフルパスが表示される。

## 注意事項

- 透過PNGをJPEGに変換すると、透明部分は白背景に変換される。
- 変換先に同名のファイルが既に存在する場合は上書きされる。
- 日本語を含むファイル名・フォルダ名にも対応している。
- 日本語フォント（游ゴシック / メイリオ / MSゴシックのいずれか）がシステムに見つからない場合、UIの日本語文字が正しく表示されないことがある。

## 開発者向け情報

ビルド方法は `CLAUDE.md` を参照。
